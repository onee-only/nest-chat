doctype 
html(lang='en')
    head
        meta(charset='utf-8')
    body
        h1 #{nickname}'s email verification.
        p this verification expires at #{expiration}
        p click this 
            a(href=`https://${domain}${path}?token=${token}`) link 